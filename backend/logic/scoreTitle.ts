/*

const scoreSkills = (jobSkills: string[], userSkills: string[]) => {
    

    if (userSkills.length === 0 || jobSkills.length === 0)
    {
        return 0;
    }

    let matchCount = 0;

    for (let i = 0; jSkills.length > i; i++) {
        for (let j = 0; uSkills.length > j; j++){
           const jSkills = jobSkills.toLowerCase().trim();
            const uSkills = userSkills.toLowerCase().trim();
        if (jSkills[i].includes(uSkills[j]))
        {
            matchCount++
        }
    }
    }

    const totalMatches = (matchCount  / jSkills.length)

    if (totalMatches === 0)
    {
        return 0
    }

    return totalMatches * 100;


};
*/